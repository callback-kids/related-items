import React from 'react';
import ReactStars from 'react-rating-stars-component';

const StarRating = () => (
  <ReactStars
    count={5}
    size={24}
    activeColor="#ffd700"
  />
);

export default StarRating;
